default_model: sonnet
mcp:
    servers:
        fetch:
            command: "uvx"
            args: ["mcp-server-fetch"]
        filesystem:
            command: "npx"
            args: ["@modelcontextprotocol/server-filesystem","."]
